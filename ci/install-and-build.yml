install and build:
    stage: setup environment
    script:
        - yarn install --pure-lockfile --cache-folder .yarn --modules-folder /cache/${CI_COMMIT_REF_SLUG}/node_modules/
        - yarn run build:libs